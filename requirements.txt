# Typeless-Linux 依赖
# 语音识别 (FunASR 运行时)
funasr>=1.0.0
websocket-client>=1.6.0

# AI 文本润色 (DeepSeek API - OpenAI 兼容)
openai>=1.0.0

# 音频捕获
pyaudio>=0.2.11

# 全局热键监听
pynput>=1.7.0

# 系统托盘
pystray>=0.19.0
Pillow>=10.0.0

# 剪贴板操作
pyperclip>=1.8.0

# 设置界面 (使用 Python 内置 tkinter，无需额外安装)
